.env
venv/
venv311/
__pycache__/
*.pyc
.aider*

# Private working notes (not for the public repo)
sample/
ROADMAP.md

# Dataset assets (large / may be copyrighted assessment material)
ai_agent_dataset/
ai_agent_dataset.zip
__MACOSX/

# Generated scan output (regenerated by run.py) — but track the bundled
# sample inside the skill folder so reviewers see what output looks like.
quality_report.json
quality_report.md
!dataset/financial-data-quality/quality_report.md

# Submission zip we rebuild on demand
financial-data-quality-skill*.zip

# Local dev venv for agent_graph
.venv-dev/

# CI worktree for the gh-pages publish branch
.gh-pages/
