# Runtime dependencies for agent_graph.
anthropic>=0.122
langgraph>=1.1
pydantic>=2.9
fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0

# Optional: run the graph on a Claude subscription instead of an API key.
# Also needs the Claude Code CLI (`claude setup-token` to authenticate).
# pip install claude-agent-sdk

# Tests
pytest>=8.0
